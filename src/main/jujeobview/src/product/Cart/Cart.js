function Cart(){
    return(
        <div>
            {/*{cartItems.map((item, index) => (
                <li key={index}>{item.name}</li>
            ))}*/}
        </div>
    )
}
export default Cart;